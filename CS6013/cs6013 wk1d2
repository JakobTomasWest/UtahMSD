#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

typedef struct node {
  int data;     //integer is 4 bytes
  //just want a reference to a node not to recurvely create nodes so we use*
  struct node * next; //pointer&addresses are 8 bytes 64 bits
} Node;

void print_list(Node*head){ //(1) Node**head
  while( head != NULL){
  printf("Data: %d\n", head->data);
  head = head->next;
  }
}

void create_image(){
  //open file
  printf(fp, "  o    o  \n");
  printf(fp, "    ^\n ")
  printf(fp, "  \    /\n");
  printf(fp, "   ----  \n");


}




int
main()
{
  // struct node the_node;
  Node * the_node = malloc(sizeof(Node));

  printf("the_node used %lu bytes\n"), sizeof( node));
  the_node->data =3;
  the_node->next = NULL;

  Node * the_node = malloc (sizeof(Node));
  if( the_node ==NULL){
    printf("Error,malloc failed\n");
    exit(-1);
  }

  printf("hello world: %d\n", the_node->data );

  Node * secondNode = malloc ( sizeof(Node));
  secondNode->data =4;
  secondNode->next = NULL:

  the_node->next = secondNode;

  print_list( the_node);  //(1) &the_node

  return 0;

}
